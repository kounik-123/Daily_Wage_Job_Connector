<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Daily Wage Jobs</title>

  <style>
    body {
      background: #1e293b;
      color: white;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    h2 {
      margin-bottom: 20px;
    }

    .active-jobs-container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }

    .job-card {
      background-color: #0f172a;
      padding: 16px;
      border-radius: 12px;
      width: calc(33.33% - 20px);
      box-sizing: border-box;
      box-shadow: 0 0 20px 4px rgba(37, 99, 235, 0.4);
      transition: transform 0.3s ease;
    }

    .job-card h4 {
      margin: 0;
      font-size: 18px;
      margin-bottom: 10px;
    }

    .content_1 p {
      font-size: 14px;
      color: #cbd5e1;
      margin-top: 10px;
    }

    .content_boxes {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
      margin-top: 10px;
    }

    .content_boxes > div {
      background-color: #1e293b;
      padding: 8px 12px;
      border-radius: 6px;
      font-size: 13px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .tag {
      margin-top: 10px;
      display: flex;
      align-items: center;
      gap: 6px;
      color: #38bdf8;
      font-size: 13px;
    }

    .buttons {
      display: flex;
      justify-content: space-between;
      margin-top: 12px;
      gap: 10px;
    }

    .apply_now,
    .view_details {
      height: 40px;
      border-radius: 6px;
      cursor: pointer;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 14px;
      font-weight: 500;
      transition: transform 0.2s ease;
      position: relative;
      overflow: hidden;
    }

    .apply_now {
      flex: 1;
      background-color: rgb(85, 85, 226);
      border: none;
    }

    .apply_now p {
      color: white;
      margin: 0;
      z-index: 2;
      position: relative;
    }

    .apply_now::before {
      content: '';
      position: absolute;
      top: -100%;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: #fddede;
      z-index: 1;
      transition: top 0.3s ease;
    }

    .apply_now:hover::before {
      top: 0;
    }

    .apply_now:hover p {
      color: #d10000;
    }

    .view_details {
      background-color: rgb(85, 85, 226);
      border: 1px solid grey;
      width: 220px;
      text-decoration: none;
    }

    .view_details p {
      color: #333;
      margin: 0;
      z-index: 2;
      position: relative;
      transition: color 0.4s ease;
    }

    .view_details::before {
      content: '';
      position: absolute;
      top: 0;
      left: -10%;
      width: 120%;
      height: 100%;
      background-color: white;
      transform: skew(30deg) translateX(0);
      transition: transform 0.4s cubic-bezier(0.3, 1, 0.8, 1);
      z-index: 1;
    }

    .view_details:hover::before {
      transform: skew(30deg) translateX(100%);
    }

    .view_details:hover p {
      color: white;
    }

    @media screen and (max-width: 1024px) {
      .job-card {
        width: calc(50% - 20px);
      }
    }

    @media screen and (max-width: 768px) {
      .job-card {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <h2>All Jobs</h2>

  <form method="GET" action="/jobListing/new">
    <button style="margin-bottom: 20px;">Create New Job</button>
  </form>

  <div class="active-jobs-container">
    <% for(let Job of allJobs) { %>
      <div class="job-card">
        <div style="display: flex; justify-content: space-between; align-items: center;">
          <h4><%= Job.jobTitle %></h4>
          <div style="display: flex; gap: 8px;">
            <% if(Job.isUrgent) { %>
              <span style="color: #f87171; font-size: 12px; background: rgba(255, 0, 0, 0.15); padding: 2px 6px; border-radius: 6px;">Urgent</span>
            <% } %>
            <% if(Job.isAvailable) { %>
              <span style="color: #10b981; font-size: 12px; background: rgba(16, 185, 129, 0.15); padding: 2px 6px; border-radius: 6px;">Available</span>
            <% } %>
          </div>
        </div>

        <div class="content_1">
          <p><%= Job.jobDescription %></p>
        </div>

        <div class="content_boxes">
          <div><i class="uil uil-map-marker"></i><p><%= Job.jobLocation %></p></div>
          <div><i class="uil uil-rupee-sign"></i> ₹ <p><%= Job.jobPay %></p></div>
          <div><i class="uil uil-clock"></i><p><%= Job.jobTime %></p></div>
          <div><i class="uil uil-calendar-alt"></i><p><%= Job.jobDate.toDateString() %></p></div>
        </div>

        <div class="tag">
          <i class="uis uis-bookmark"></i>
          <p><%= Job.jobTags %></p>
        </div>

        <div class="buttons">
          <div class="apply_now"><p>Apply Now</p></div>
          <a class="view_details" href="/JobListing/<%= Job._id %>"><p>View Details</p></a>
        </div>
      </div>
    <% } %>
  </div>

</body>
</html>


